// Usage example for ES Module (import syntax)

import { areaOfCircle, circumferenceOfCircle, volumeOfSphere, volumeOfCone } from './geometry.js';

console.log(exactAreaOfCircle(2));         // 12.8
console.log(exactCircumferenceOfCircle(2)); // 12.8
console.log(exactVolumeOfSphere(2));        // ≈28.84
console.log(exactVolumeOfCone(2, 5));       // ≈11.31
